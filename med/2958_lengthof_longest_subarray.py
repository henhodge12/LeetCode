class Solution(object):
    def maxSubarrayLength(self, nums, k):
        r, l, max = 0, 0, 0

        n = len(nums)
        while r < n:

        """
        :type nums: List[int]
        :type k: int
        :rtype: int
        """
